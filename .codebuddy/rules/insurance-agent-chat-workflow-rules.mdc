---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-21T03:11:03.024Z
provider: 
---

# 保险代理人助手对话编排与规则收口硬规则

## 适用范围
- 本规则适用于保险代理人助手的意图识别、信息提取、页面承接、确认机制、任务编排、跨流程恢复与多意图处理。
- 后续凡涉及助手对话、流程跳转、结构化承接页、任务生成、客户建档、拜访记录、活动补录、查询与提醒的设计、实现、测试与验收，都必须优先对照本规则。

## 总体方案
- 采用 **方案C：大模型理解 + 规则收口**。
- 大模型负责理解，不负责直接执行。
- 系统负责确认、校验、编排与恢复。
- 业务服务负责最终执行与写库。

## 角色分工
### 大模型负责
- 意图识别
- 信息提取
- 缺失项判断
- 多意图拆分
- 情绪识别与回应建议
- 下一步建议与过渡话术建议

### 规则与业务服务负责
- 字段校验
- 数据格式标准化
- 客户查重与客户存在性判断
- 是否允许保存
- 是否必须确认
- 任务生成规则
- 页面跳转规则
- 草稿恢复与跨流程返回
- 最终写库、更新、删除与查询执行

## 8条硬规则

### 规则1：模型只输出结构化建议，不直接写库
- 大模型只能输出结构化理解结果，例如 `intent`、`slots`、`missingFields`、`confidence`、`suggestedSurface`。
- 大模型不得直接执行新增、修改、删除、关联、关闭任务等写操作。
- 所有执行动作都必须进入后续规则收口与业务服务层。

### 规则2：所有写操作必须走业务服务
- 新增客户必须走客户服务。
- 保存拜访必须走拜访服务。
- 补录活动必须走活动服务。
- 生成提醒或任务必须走任务服务。
- 不得在聊天逻辑中直接操作数据库。

### 规则3：高风险动作必须确认
以下情况必须先确认，再执行：
- 删除数据
- 覆盖或修改已有敏感客户信息
- 多客户活动关联
- 自动生成或关闭任务
- 低置信度识别结果下的写操作
- 任何可能引发数据误写、误关联、误删除的动作

### 规则4：字段合法性以本地规则为准
- 日期、姓名、必填项、字段长度、枚举值等合法性，以本地规则和业务校验为准。
- 即使大模型已提取字段，也必须经过本地标准化与校验。
- 大模型输出与本地规则冲突时，以本地规则为最终依据。

### 规则5：模型抽取失败时允许降级，但不能乱填
- 当大模型无法稳定提取完整信息时，可以只预填部分字段。
- 可以提示用户补充，但不得脑补、编造或替用户假设事实。
- 尤其不得凭空补出地点、客户诉求、后续动作、参与客户、时间等关键信息。

### 规则6：所有跨流程场景必须可恢复
- 凡存在前置依赖的流程，系统必须支持自动补依赖并返回原流程。
- 例如：保存拜访时若客户未建档，应先建档，再自动回到拜访保存。
- 例如：补录活动时若参与客户缺失，应支持连续补录并回到原活动草稿。
- 被打断流程必须保留草稿、上下文和返回步骤。

### 规则7：意图不明确或信息不完整时，先澄清，不执行
当出现以下情况时，系统不得直接执行，也不得擅自假设用户意思：
- 意图不明确
- 同时存在多个可能意图
- 关键信息缺失
- 表达存在歧义
- 输入可能有口误、对象混淆、时间混淆或逻辑冲突

此时系统应先进入澄清对话：
- 先确认用户是想做哪件事
- 先补齐关键缺失信息
- 先允许用户纠正可能存在的输入错误

总原则：**先理解，再澄清，后执行；宁可多问一句，也不误做一步。**

### 规则8：多意图输入时，先拆分、再排序、后承接
- 当用户一次输入中包含多个意图时，系统不得混乱并发执行，而应先识别并拆分这些意图，再按优先级处理。
- 同一时刻只承接一个主工作流，其余意图作为前置依赖、后续步骤、预览建议或澄清问题处理。

#### 8.1 情绪优先
- 若多意图中包含情绪诉求，如需要赞扬、安慰、稳定节奏、缓解压力，则必须先完成情绪承接，再进入后续业务主流程。
- 情绪承接必须专业、温和、克制，不能轻浮或娱乐化。

#### 8.2 单主流程承接
- 一次只进入一个主工作区。
- 不得同时并发打开多个主写操作流程。

#### 8.3 依赖自动补齐
- 若次意图是主意图的前置依赖，应自动补齐依赖，再返回主流程。

#### 8.4 次意图排队或预览
- 非主意图不抢占当前主工作区。
- 应以下一步建议、预览区、待确认事项或保存后续动作的方式承接。

#### 8.5 冲突、歧义或主流程竞争先澄清
当多意图输入中出现以下任一情况时，系统必须停止执行并进入澄清态：
- 某个意图本身不明确，无法判断是新增、记录、查询还是修改
- 多个意图都像主意图，且当前无法安全排序主次
- 对象、时间、动作、目标之间相互矛盾
- 主意图依赖的前置信息本身存在歧义或冲突

此时系统必须遵守以下要求：
- 不得直接执行任何写操作
- 不得擅自挑选其中一个意图先执行
- 不得假设用户真正想做的是哪一项
- 必须明确指出冲突点、歧义点或待确认点，再向用户发起最小必要澄清

澄清问题应尽量直接、具体，优先确认：
- 当前要承接的唯一主流程是哪一项
- 冲突对象到底是谁
- 冲突时间以哪一个为准
- 动作到底是立即执行、先生成草稿，还是暂不执行

澄清完成后，系统仍然只能承接一个主流程；其余意图按“前置依赖 / 排队事项 / 下一步建议”处理。

总原则：**多意图可以识别，但只要存在歧义、冲突或主流程竞争，就先澄清，不执行；澄清完成后，仍一次只进入一个主流程。**

## 对话设计要求
- 助手应表现为高素质业务助理，先理解目标，再主动编排流程。
- 任何对话能力增强都不能破坏“助手驱动主流程 + 全量视图兜底 + 中断可恢复”。
- 手机端同屏只突出一个主工作区；多意图的其他部分应用轻量提示、下一步建议或结构化承接区处理。

## 验收要求
后续涉及助手能力的需求，至少应从以下维度验收：
- 是否符合方案C，而不是纯规则或纯模型直写
- 是否发生了规则收口
- 是否在不明确时先澄清
- 是否在高风险动作前确认
- 是否支持跨流程恢复
- 是否在多意图场景下保持单主流程承接
- 是否在包含情绪诉求时先完成情绪承接
- 是否在多意图冲突、歧义或主流程竞争时先进入澄清态

## 实施总原则
**助手负责理解，系统负责确认，业务服务负责执行。**