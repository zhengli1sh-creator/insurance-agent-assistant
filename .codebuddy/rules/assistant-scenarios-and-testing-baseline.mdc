---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-19T02:37:35.279Z
provider: 
---

# 保险代理人助手场景基线与测试规则

## 基本要求
- 后续交互设计、页面设计、前端开发、测试案例设计与验收时，必须优先对照本规则中的 `10` 个核心真实使用场景。
- 这 `10` 个场景按真实发生频率从高到低排序，高频场景应优先获得更顺畅、更少跳转、更少中断的体验。
- 本规则与项目既有的设计原则规则配套使用：设计原则定义整体风格与产品定位；本规则定义场景优先级、交互分层和测试基线。

## 三类交互分层规则
### A 类：助手驱动
适用于目标明确、单任务、高频、手机端优先的场景。
- 默认采用 `聊天输入 -> 意图识别 -> 主工作卡 -> 执行 -> 下一步建议`
- 用户不需要先理解页面结构
- 手机端同屏只突出一个主工作区

### B 类：混合模式
适用于可由助手发起，但必须通过结构化列表、结果区或批量操作区承接的场景。
- 默认采用 `聊天发起 -> 结构化承接 -> 执行`
- 不得把这类场景强行做成纯聊天
- 必须允许展开全量视图

### C 类：全量视图
适用于复盘、比较、趋势、总览类场景。
- 默认采用 `全量视图 / 复盘模式 + 助手解释`
- 不得压缩成单主卡或纯问答
- 必须保留结构化总览与多区块比较

## 10 个核心场景
### 1. 拜访结束后立即补录拜访并生成后续待办
- 分类：`A 类`
- 必须支持从助手入口直接发起
- 客户不存在时必须先建档，再自动返回继续保存
- 拜访草稿与后续任务预览必须可恢复

### 2. 每天早上开工先看今天该推进什么
- 分类：`A 类`
- 首屏必须能快速看到今日重点
- 应支持从建议直接进入执行

### 3. 新认识客户后先建立基础档案
- 分类：`A 类`
- 支持最小必填先保存
- 应提示重名或相似客户，避免重复建档

### 4. 活动结束后统一补录活动和参与客户
- 分类：`B 类`
- 必须支持结构化活动录入与参与客户校验
- 缺失客户需支持连续补录并回到原活动草稿

### 5. 跟进客户前快速回顾最近上下文
- 分类：`A 类`
- 应以顾问简报方式展示客户摘要、最近记录和下一步建议

### 6. 找一批适合统一经营的客户做邀约或批量触达
- 分类：`B 类`
- 助手负责发起，结果必须结构化展示
- 应支持名单查看、模板保存与复用

### 7. 处理中断已久或逾期任务
- 分类：`B 类`
- 必须支持从助手建议进入批量任务处理
- 必须保留完整任务板兜底

### 8. 压力大、思路乱时让助手帮助整理节奏
- 分类：`A 类`
- 助手应先做优先级整理，再引导进入执行流程

### 9. 把高频查询沉淀为查询模板
- 分类：`B 类`
- 模板保存应自然嵌入查询结果流程
- 后续必须便于快速复用

### 10. 周复盘 / 月复盘
- 分类：`C 类`
- 必须提供复盘模式或全量视图
- 必须支持总览、比较、趋势和建议输出

## 开发遵守规则
- 设计或开发前，必须先判断当前需求属于哪一个场景，以及对应 `A / B / C` 分类。
- 不得把批量、复盘、总览类场景误做成纯助手问答。
- 不得把高频单任务场景误做成重导航、重跳转、重表格体验。
- 所有场景实现都必须符合“助手驱动主流程 + 全量视图兜底 + 中断可恢复”。

## 测试遵守规则
- 后续测试案例设计应以这 `10` 个场景作为主清单。
- 每个场景至少覆盖：主路径测试、异常路径测试、恢复路径测试、手机端体验测试。
- 后续对首页、客户、记录、任务、洞察、复盘等模块的变更，应优先回归这 `10` 个场景。

## 验收要求
- 功能完成不等于验收通过。
- 若实现与本规则中对应场景的交互分类、核心流程或测试基线不一致，应继续调整后再验收。